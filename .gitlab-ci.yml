stages:
  - install

job1:
  tags:
    - docker
  stage: install
  script:
    - echo $RANDOM >> my-file
    - cat my-file
    - sleep 300
    - cat my-file
  artifacts:
    name: my-files
    paths:
      - my-file
